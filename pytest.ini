[pytest]
addopts = -v --html=reports/report.html --self-contained-html
testpaths = tests
pythonpath = .
markers =
    api: API validation tests
    regression: Baseline regression and drift detection tests
    performance: Latency and execution time tests
    pipeline: Data pipeline validation tests
    schema: JSON schema validation tests
    aiworkflow: AI workflow simulation and logic tests
